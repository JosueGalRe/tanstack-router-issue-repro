import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_auth/waste-and-breakdowns-log/')({
  component: () => <div>Hello /waste-and-breakdowns-log/!</div>,
});
