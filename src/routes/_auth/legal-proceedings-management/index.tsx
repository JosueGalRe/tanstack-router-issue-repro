import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_auth/legal-proceedings-management/')({
  component: () => <div>Hello /legal-precidings-control/!</div>,
});
